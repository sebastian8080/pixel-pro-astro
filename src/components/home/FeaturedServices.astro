---
// src/components/home/FeaturedServices.astro
import ServiceCard from '../services/ServiceCard.astro';
import { services } from '../../data/services'; // Importamos todos los servicios

// Puedes decidir qué servicios mostrar aquí.
// La imagen muestra 6 servicios, si tu array 'services' tiene más, ajusta slice.
const displayedServices = services.slice(0, 6);
---
<section class="py-12 md:py-20 bg-blue-900 text-white shadow-xl mt-14">
    <div class="container mx-auto px-4 md:px-8">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-12 md:gap-16 items-start">
            <div class="text-center md:text-left p-4 lg:sticky lg:top-28">
                <h2 class="text-4xl md:text-5xl font-extrabold text-white mb-6 leading-tight">
                    Servicios de <br class="hidden md:inline"> Marketing Digital
                </h2>
                <p class="text-lg md:text-xl text-blue-200 leading-relaxed mb-8">
                    Nuestra <strong>agencia de mkt digital</strong> (abreviando en términos cortos) ofrece soluciones de marketing digital diseñadas para potenciar tu negocio. Desde Google Ads y SEO hasta gestión de redes sociales, diseño web y pautas en Meta, nos especializamos en crear estrategias efectivas para aumentar tu visibilidad y atraer más clientes.
                </p>

                <a class="inline-block bg-blue-600 text-white font-bold py-3 px-8 rounded-full text-lg hover:bg-blue-700 transition duration-300 ease-in-out transform hover:scale-105 shadow-lg" href="/servicios">Ver todos los servicios</a>

                </div>

            <div class="grid grid-cols-1 sm:grid-cols-2 gap-6 p-4">
                {displayedServices.map(service => (
                    <ServiceCard service={service} isDarkBackground={true} />
                ))}
            </div>
        </div>
    </div>
</section>